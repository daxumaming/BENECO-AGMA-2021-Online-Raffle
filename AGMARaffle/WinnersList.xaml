<Window x:Class="AGMARaffle.WinnersList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AGMARaffle"
        mc:Ignorable="d"
        Title="WinnersList" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="listWinners" AlternatingRowBackground="#FFF0F0F0" Grid.Column="3" Grid.RowSpan="5" FontSize="20" ItemsSource="{Binding Path=LoadDataBinding}" >
            <DataGrid.Columns>
                <!--
                <DataGridTextColumn Header="MCO Name" Binding="{Binding mconame}" Width="*"/>
                <DataGridTextColumn Header="Email Address" Binding="{Binding emailaddress}" Width="*"/>
                <DataGridTextColumn Header="Prize Won" Binding="{Binding prizewon}" Width="*"/>
                <DataGridTextColumn Header="Draw Time" Binding="{Binding drawtime}" Width="*"/>
                -->
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Window>
